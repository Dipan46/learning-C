#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{

    int n, i, j, k;
    scanf("%d", &n);

    for (i = 0; i < +n; i++)
        printf("%d\n", n);
    for (i = 0; i < +n; i++)
        printf("%d", n);
    for (i = 0; i < +n; i++)
        printf("%d\n", n);
    return 0;
}
