#include <stdio.h>

int main()
{
    char in[100], out[100];
    printf("Enter anything: ");
    scanf("%[^\n]s", in);
    for (int i = 0; i <= 100; i++)
    {
        
    }
    printf("%s", out);
    return 0;
}
